version: '2'
services:
  mysql:
    image: mysql:latest
    volumes:
      - "./.mysql-data/db:/var/lib/mysql"
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: kjio0988
      MYSQL_DATABASE: offpayapdb1
      MYSQL_USER: MT
      MYSQL_PASSWORD: kjio0988